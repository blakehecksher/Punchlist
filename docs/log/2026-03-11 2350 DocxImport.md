# 2026-03-11 2350 DocxImport

## TL;DR
- What changed: Added Word `.docx` import support, updated the import UI to present Word as the primary file format, and lazy-loaded the Word parser so the main app bundle stays small.
- Why: In practice, Word files are more likely than markdown or plain text for field-note exchange, so the importer should support `.docx` directly.
- What didn't work: Legacy `.doc` is still not supported; users need to save as `.docx`, `.md`, or `.txt`.
- Next: Test a real exported Word outline to confirm text extraction preserves the bullet structure cleanly enough for the existing parser.

---

## Full notes

- Installed `mammoth` for client-side `.docx` text extraction.
- Added `src/importFile.js` to:
  - reject legacy `.doc` with a clear message
  - extract raw text from `.docx`
  - fall back to plain text loading for `.md` and `.txt`
- Updated `src/PunchListApp.jsx` import copy and file picker text so Word `.docx` is the primary documented file import option.
- The Word parser is loaded with a dynamic import, so it only downloads when the user selects a `.docx` file.
- Verification:
  - `npm run build` passed
