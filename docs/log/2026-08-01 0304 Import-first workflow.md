# 2026-08-01 0304 Import-first workflow

## TL;DR
- What changed: Reframed onboarding and help around drafting elsewhere, importing
  a nested bullet outline, adding photos, and printing the resulting document.
- Why: Punch List is the numbering, photo, and publishing step—not the primary
  writing environment.
- What didn't work: The first sidebar spacing pass still let the negative label
  margin overlap the disclosure header; the final rule now leaves a measured
  10px gap.
- Next: Validate with a real Word outline and a folder of project photos.

---

## Full notes

- Removed Add Room from the top toolbar.
- Restored Print / PDF as the dark primary toolbar action.
- Rewrote the startup card around `write elsewhere → import → drag photos → PDF`.
- Added a compact bullet-format example and copyable outline template.
- Rewrote How It Works and import guidance around the same workflow.
- Added persistent `Add photo / Click or drag and drop` guidance to empty photo
  cells while retaining `No Photo Provided` in print.
- Fixed the Document Settings disclosure spacing so Cards per page no longer
  overlaps the hovered/open header.
- Verified imported room `Kitchen 102`, issue `102-01`, photo accessibility text,
  photo drop-zone copy, and sidebar spacing in the local browser.
